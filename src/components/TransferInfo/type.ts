export interface INavItem {
    id: number;
    name: string;
}

export interface IListItem {
    id: number;
    name: string;
    description: string;
    imgPath: string;
    className?: string;
}
